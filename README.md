# iot-vas
So far the create target and create task work and the IDs are displayed in the console. To create a target you enter a name and the ip in the hosts section. When creating a task you need to eneter a name (which does not matter), the type of scan, the config of the scan and the target Id returned when you create a target in the console.

Now working on starting the task and viewing the progress. 
